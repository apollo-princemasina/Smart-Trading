# CalendarEvent has been replaced by the canonical market_intel.models.MFIPEvent.
# This file is intentionally empty — it is kept so git history records the transition.
#
# All code that previously imported CalendarEvent should import MFIPEvent instead:
#   from market_intel.models import MFIPEvent
